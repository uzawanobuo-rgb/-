# 価格一括更新ファイル × 【全国】おためし入居キャンペーン シート同期結果

価格一括更新ファイルの「おためし」シートに、【全国】おためし入居キャンペーンファイルの最新シート（`260804`＝2026-08-04）の内容を反映しました。

## マッピング

### ①新規依頼（【全国】おためし入居キャンペーン:M列 ＝ 価格一括更新ファイル:D列 でVLOOKUP）
| 項目 | 【全国】おためし入居キャンペーン列 | 価格一括更新ファイル列 |
|---|---|---|
| 期間開始日 | N列 | AG列（入居可能開始日） |
| 期間終了日 | P列 | AH列（入居可能終了日） |
| 賃料 | V列 | Z列（賃料）※数式 `=N{行}*(1-{割引率}%)` で記載（例：`=N936*(1-50%)`。定価Nから割引率分を引いた実額を自動計算） |
| 清掃費 | W列 | AF列（割引率）※数値0.75などに変換して記載（セルが0%書式のため） |
| 更新対象マーク | - | AK列に「○」 |

### ②期間変更（【全国】おためし入居キャンペーン:A列 ＝ 価格一括更新ファイル:D列 でVLOOKUP）
| 項目 | 【全国】おためし入居キャンペーン列 | 価格一括更新ファイル列 |
|---|---|---|
| 期間開始日 | B列 | AG列（入居可能開始日） |
| 期間終了日 | D列 | AH列（入居可能終了日） |
| 賃料 | 価格一括更新ファイル:R列（自身の現行おためし価格） | Z列（賃料） |
| 更新対象マーク | - | AK列に「○」 |

※ご指定いただいたAP/AD/AJ列は「おためし」シートには実在しない、または既存の別データ（現：価格・最終退去日）が入っている列だったため、実際のシート構成に合わせてAK/Z/AFに変更しています（チャットでのやり取りで確認済み）。

## 結果

- ①新規依頼: 18件中 **18件マッチ** → ○を記載
- ②期間変更: 2件中 **2件マッチ** → ○を記載

VLOOKUPはまず完全一致で照合し、一致しない場合は価格一括更新ファイル側プラン名の先頭記号（◇◆■□●☆）を除いた名前で再照合するフォールバックを追加しています（【全国】おためし入居キャンペーン側で記号の入力漏れがあるケースに対応。価格一括更新ファイル側の記号が正のため、そちら側は変更せずマッチングのみ緩和）。この結果、以下の2件も更新されました。

- `アットイン中野9-2`（価格一括更新ファイル側は「□アットイン中野9-2」）→ 記号なしで再照合しマッチ
- `アットイン武蔵小杉2-1`（価格一括更新ファイル側は「◇アットイン武蔵小杉2-1」）→ 記号なしで再照合しマッチ

先頭記号を除いた名前が複数の価格一括更新ファイル行と一致してしまう「あいまい一致」が発生した場合は自動更新せず、スクリプトの実行ログに候補行を出力します（今回は該当なし）。

## 注意事項

- このセッションのLibreOffice環境が正常に動作せず（どのファイルも読み込めない状態）、自動フォーマチュラ再計算・検証ができませんでした。書き込んだ数式・値については手計算でExcel/Calcの数式評価ルールに基づきエラー（#VALUE!等）が発生しないことを確認していますが、実際にExcelで開いて最終確認をお願いします。
- Z列（賃料）の数式 `=N*(1-割引率%)` は、ユーザー確認済みの解釈（「60%OFF」＝定価の60%引き＝定価の40%）に基づいています。
- 「スペシャル」「ロング」「CP価格設定シート」の各シートは変更していません。
- マクロ（VBA）は保持されています。

## Webツール

毎回コマンドを打たなくても、ブラウザからファイルをアップロードして実行できるツールを `webtool/` に用意しました。社外への公開はせず、**各自のPCでダブルクリックして起動する**方式です（VBAマクロを保持したままにするにはPython実行環境が必要なため、GitHub Pages等の静的公開ではマクロが失われてしまいます）。

- Windows: `webtool/起動.bat` をダブルクリック（スマートアプリコントロール等でブロックされる場合は、`webtool/同僚向けセットアップ手順.md` 記載の手動起動コマンドを利用）
- Mac: `webtool/起動.command` をダブルクリック

自動でブラウザが開き、【全国】おためし入居キャンペーン/価格一括更新ファイルをアップロードするとマッチ結果一覧の確認後に更新済みファイルをダウンロードできます。詳細・トラブルシューティングは `webtool/README.md` および `webtool/同僚向けセットアップ手順.md` を参照してください。

サーバー側（Python/openpyxl）で処理するため、VBAマクロ・条件付き書式・他シートはすべて保持されます。アクセスは`127.0.0.1`（自分のPC内）のみに限定しています。

## ファイル

- `価格一括更新ファイル.xlsm` — 更新後のファイル（最新の同期結果）
- `sync_core.py` — 同期ロジック本体（CLI・Webツール共通）
- `sync_cp_otm.py` — CLI版（`python sync_cp_otm.py <OTM.xls> <CP.xlsm> <出力.xlsm>`）
- `webtool/` — ローカルWebツール（Flask）。`起動.bat`(Windows) / `起動.command`(Mac) でダブルクリック起動
